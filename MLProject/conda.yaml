name: loan_approval_env

channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.12.7
  - pip
  - pip:
      - pandas
      - numpy
      - scikit-learn==1.5.2
      - matplotlib
      - mlflow==2.19.0